// This file was auto-generated by SourceMod (v1.11.0.6453)
// ConVars for plugin "Guideline"
//
// 注意：本文件为仓库提供的默认模板。服务器首次启动时 autoexecconfig
// 会执行本文件并把缺失的 ConVar 自动补齐（含注释与默认值），
// 因此直接编辑本文件即可完成配置。

// 总开关：是否启用 guideline 路线显示功能（关闭后 !gl 无效）。
gokz_guideline_enabled "1"

// R2 基础 URL（域名 + 可选前缀，不含路径参数）。
// 插件自动拼接完整录像地址：{base}/wr/<模式>/<地图>/<pro|tp>.replay
// 与 stratosphere 上传插件键名约定完全一致。
gokz_guideline_url "https://cngokzreplay.iquankz.cn"

// R2 鉴权密钥（与 stratosphere 上传 Worker 约定的 X-API-Key）。
gokz_guideline_api_key ""

// 地图加载时是否自动检查并加载最快路线。
gokz_guideline_auto_check "1"

// 调试日志开关。
gokz_guideline_debug "0"

// 路线线条颜色 "R G B A"（0-255）。默认为紫色。
gokz_guideline_color "148 0 211 110"

// 线条存活时间（秒），与 GOKZ JumpBeam 一致。
gokz_guideline_beam_lifetime "4.0"

// 线条宽度（与 GOKZ JumpBeam 一致）。
gokz_guideline_beam_width "0.25"

// 路线重发光束的刷新间隔（秒），需小于 beam_lifetime 避免闪烁。
gokz_guideline_refresh_interval "2.0"

// 路线平滑开关：Chaikin 角切割细分，拐角处切出圆角（与 JumpBeam 视觉一致）。
gokz_guideline_smooth "1"

// Chaikin 细分迭代次数（0-3）：越大越圆润，每迭代一次段数约 ×2。
gokz_guideline_smooth_points "1"

// 轨迹降采样距离阈值（units）：相邻保留点水平距离小于该值则丢弃（起跳点除外）。
gokz_guideline_sample_dist "32.0"

// 轨迹断点判定距离（units）：相邻两点 3D 距离超过该值视为断点（传送/异常），绘制时断开。
gokz_guideline_break_dist "1000.0"

// 双层断点判定距离（units）：相邻两点水平距离 < 64 但垂直距离超过该值视为断点（上下层错位），绘制时断开。
gokz_guideline_vertical_break_dist "300.0"

// 完整路线最大绘制线段数上限（保护上限，超出则自动降低平滑迭代仍保持全图）。
gokz_guideline_max_segments "2000"

// 录像解析每批处理的帧数（分帧解析防止服务器卡顿）。
gokz_guideline_parse_batch "5000"

// R2 meta 查询超时（秒）。
gokz_guideline_meta_timeout "10"

// R2 录像下载超时（秒）。
gokz_guideline_download_timeout "30"
